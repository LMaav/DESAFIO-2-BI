<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Precisamos de internet para falar com o Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Estacionamento">

        <!-- PerfilActivity e a tela inicial (LAUNCHER): escolhe Operador ou Cliente -->
        <activity
            android:name=".ui.PerfilActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.SplashActivity" />
        <activity android:name=".ui.ClienteAreaActivity" />
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.CadastroActivity" />
        <activity android:name=".ui.HomeActivity" />
        <activity android:name=".ui.GerenciarVagasActivity" />
        <activity android:name=".ui.CadastrarCarroActivity" />
        <activity android:name=".ui.DetalhesCarroActivity" />
        <activity android:name=".ui.ClientesActivity" />

    </application>

</manifest>
